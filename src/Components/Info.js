import React, { Component } from "react";
import { View, Text } from "react-native";

class Info extends Component {
  state = {
    info: []
  };
  render() {
    return <View />;
  }
}

export default Info;
